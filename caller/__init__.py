"""Method-specific annotation callers.

Callers own adaptation to upstream packages, languages, and model APIs.  They
consume the label-free task contract produced by :mod:`runner` and emit its
standard prediction TSV contract.
"""
